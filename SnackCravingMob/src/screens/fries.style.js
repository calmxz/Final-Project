import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        width: "100%"
    },
    welcomeTxt:{
        fontWeight: "bold",
        fontSize: 35,
        marginTop: 60,
        color: "#000000",
        marginHorizontal: 12
    },
    containers: {
        marginTop: 50, 
        marginLeft: 12,
    },
    separator: {
        height: 40
    }
})

export default styles